export default {
  // this creates a ‘Components’ folder and a ‘MyComponent’ subfolder
  title: 'components/pix-button',
};

const template = (args) => {
  return `<pix-button></pix-button>`;
}

export const firstDisplay = template.bind({});
